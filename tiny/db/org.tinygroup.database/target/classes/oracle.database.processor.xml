<processors language="oracle">
	<processor name="table" bean="oracleSqlProcessor"></processor>
	<processor name="initData" bean="initDataSqlProcessor"></processor>
	<processor name="view" bean="viewSqlProcessor"></processor>
	<processor name="procedure" bean="oracleProcedureSqlProcessor"></processor>
	<processor name="sequence" bean="oracleSequenceSqlProcessor"></processor>
	<processor name="trigger" bean="oracleTriggerSqlProcessor"></processor>
</processors>